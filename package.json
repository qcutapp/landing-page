{
  "scripts": {
    "develop": "parcel src/*.html",
    "build": "parcel build src/*.html",
    "deploy": "parcel build --public-url '.' src/*.html && gh-pages -d dist"
  },
  "dependencies": {
    "aos": "^2.3.4",
    "bootstrap": "^4.5.0",
    "gh-pages": "^3.1.0",
    "jquery": "^3.5.1",
    "parcel": "^1.12.4",
    "parcel-plugin-imagemin": "^4.0.1",
    "query": "^0.2.0",
    "sass": "^1.26.9"
  }
}
